const multer = require("multer");
const Account = require("../model/account");
const fs = require("fs");
const path = require("path");

const handleNewUserDetails = async (req, res) => {
  //   console.log(req);
};

module.exports = { handleNewUserDetails };
